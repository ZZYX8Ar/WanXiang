# ============================================================
# 万相 MYRIAD · Unity 工程 .gitignore
# Unity 2022.3.62f3c1 / Universal 2D
#
# 原则：只提交「人写的」与「工程配置」，不提交「机器生成的」。
# 判据：删掉这个文件，Unity 重新打开工程后能否自动重建？
#   能重建（Library/、*.csproj、Temp/）→ 忽略
#   不能重建（ProjectSettings/、Assets/、Packages/manifest.json）→ 提交
# ============================================================

# ---------- Unity 自动生成的目录（体积大，可完全重建）----------
[Ll]ibrary/
[Tt]emp/
[Oo]bj/
[Bb]uild/
[Bb]uilds/
[Ll]ogs/
[Uu]ser[Ss]ettings/
[Mm]emoryCaptures/
[Rr]ecordings/

# Unity 崩溃日志
sysinfo.txt
crashlytics-build.properties

# ---------- 编译产物 ----------
# *.csproj / *.sln 是 Unity 每次刷新时重新生成的，
# 提交它们会让 diff 被噪音淹没（Guid 与文件列表每次都变）。
*.csproj
*.unityproj
*.sln
*.suo
*.tmp
*.user
*.userprefs
*.pidb
*.booproj
*.svd
*.pdb
*.mdb
*.opendb
*.VC.db

# ---------- 构建输出 ----------
# 注意：Assets/Plugins 下的 DLL（DOTween.dll 等）属于第三方源码分发，
# 必须提交，所以这里只忽略根目录与常见构建输出目录。
/[Bb]uild/
/[Bb]uilds/
/[Bb]in/
/[Oo]bj/
*.apk
*.aab
*.app
*.ipa

# ---------- 系统文件 ----------
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
ehthumbs.db
desktop.ini

# ---------- 本工程特有 ----------

# QFramework 的包列表网络请求缓存。
# 内容是从 liangxiegame.com 拉回来的插件市场列表，随时可重建，
# 且里面带有本机的绝对路径，不该进版本库。
# ⚠ 注意区分：Assets/QFrameworkData/ 是**要提交**的（含 CodeGenKit 与
#   ProjectConfig，那是工程配置），只有下面这个 .qframework 目录是缓存。
/Assets/.qframework/

# 本地导入的 .unitypackage 压缩包（DOTween 等）。
# 包内容本身已解压进 Assets/Plugins，压缩包不必入库。
*.unitypackage

# 导表工具链的中间产物与本地输出
/LocalTools/Luban/*/obj/
/LocalTools/Luban/*/bin/
/LocalTools/Luban/.cache/
/ConfigExport/

# YooAsset 本地构建产物（P4 接入后生效）
/[Bb]undles/
/BuildReport/
/AssetBundleReport/

# HybridCLR 的工作目录。
# 装 HybridCLR 环境时，它会把 Unity 自带的 il2cpp 复制到
# HybridCLRData/LocalIl2CppData-{平台}/ 再打补丁（这样不必去改
# Program Files 下的 Unity 安装，免得要管理员权限）。
# 里面还放 AOT 裁剪后的 dll、生成的热更 dll、以及 il2cpp 编译中间产物。
# 全部可由「重新安装 HybridCLR 环境 + 重新构建」重建，体积数百 MB，
# 绝对不能进版本库。
/HybridCLRData/
# HybridCLR 在 Assets 之外生成的产物目录（不同版本位置略有差异，一并忽略）
/[Hh]ybridCLRGenerate/
/AssembliesPostIl2CppStrip/
/StrippedAOTDlls/

# 热更产物（生成的二进制）。
# 「万相/热更/发布热更产物」会把 HybridCLR 编译出的热更 dll、以及裁剪后的
# AOT 元数据 dll 复制到这里，并给它们加 .bytes 后缀
#（因为 Assets 下裸的 .dll 会被 Unity 当托管插件导入，导致同一程序集编译两次）。
# 它们可由一条命令完全重建：
#     万相/热更/发布热更产物
# 为什么不能入库：
#   1. 是**构建产物**，与当前源码、Unity 版本强绑定，入库后会悄悄过期，
#      而"过期"在这里的表现是运行期加载到旧代码 —— 极难排查；
#   2. 热更 dll 的"版本"应该由**发布时间**决定，不该由 git 提交时间决定；
#   3. AOT 元数据一份近百 KB、每次发布都变，进库只会产生无意义的二进制 diff。
# 与之配套的约定：YooAsset 收集器里的「HotUpdate」组**只在目录有内容时存在**
#      （见 YooAssetSetupTool.EnsureHotUpdateGroup），
#      所以 clone 下来不会出现"配置里有组、磁盘上没目录"的不一致状态。
/Assets/WanXiangRes/HotUpdate/
/Assets/WanXiangRes/HotUpdate.meta

# 临时诊断补丁：编辑器诊断通道写出的 csc 响应文件。
# 只有一个内容：UnityEngine 门面引用 + 额外编译宏，用来绕开
#   Error building Player: Currently selected scripting backend (IL2CPP) is not installed.
# 为什么不能入库：
#   1. 第一行是 -r:"C:/Program Files/Unity/Hub/Editor/<本机版本>/..." 的**绝对路径**，
#      换台机器、换个 Unity 安装位置就失效；
#   2. 它是「缺 IL2CPP 模块」的临时绕行手段，提交了会掩盖真问题，
#      别人 clone 下来会以为工程本来就要这么配。
# 正式修复：Unity Hub 里给编辑器装上 Windows Build Support (IL2CPP)，
#          然后跑诊断通道的 csc.defines.off + facade.clear 把它清掉。
# ⚠ 只在 Assets 根目录忽略：这是诊断通道唯一会写 .rsp 的位置，
#   不影响工程内其他目录将来可能出现的正当 rsp 文件。
#   连带忽略它的 .meta —— 否则会提交一个「有 meta 无资源」的孤儿文件，
#   别人 clone 后 Unity 会把它当残留清掉，白白产生一次 diff。
/Assets/*.rsp
/Assets/*.rsp.meta

# 热更 DLL 的本地拷贝（P4 接入后生效，正式产物走 CDN 不进库）
/HotfixDlls/
*.hotfix.dll

# MCP for Unity 的运行期状态（端口、日志、临时握手文件）
# 注意：Packages/manifest.json 里的 MCP 依赖要提交，但运行期状态不提交。
/MCPForUnity/
*.mcp-state.json
mcp-for-unity*.log

# 编辑器布局与窗口位置（每人屏幕不同，提交只会互相覆盖）
*.dwlt
*.layout

# ---------- 显式保留（防止被上面的通配符误伤）----------
!.vscode/extensions.json
!.vscode/launch.json
!.vscode/settings.json
!/Docs/
!/Tools/

# ============================================================
# 维护提示
# ------------------------------------------------------------
# 新增忽略规则时，用 git check-ignore -v <文件> 确认命中了哪一条。
# 若发现本该提交的文件被忽略（例如某个 DLL），
# 用 git add -f <文件> 强制加入，并在此处补一条 ! 例外。
# ============================================================
